# QuickCopy

자주 쓰는 문구(주소, 계좌번호, 이메일 서명, 인사말 템플릿 등)를 저장해 두고
필요할 때 **한 번의 탭/클릭으로 클립보드에 복사**하는 앱입니다.

서버가 없고, 모든 데이터는 **이 기기의 브라우저 안에만** 저장됩니다.
어떤 데이터도 외부로 전송되지 않습니다.

---

## 1. 파일 구성

| 파일 | 역할 |
|---|---|
| `index.html` | 앱 전체 (HTML + CSS + JS 인라인) — 이 파일만 열어도 동작 |
| `manifest.webmanifest` | PWA 매니페스트 (앱 이름·아이콘·표시 모드) |
| `sw.js` | Service Worker (오프라인 캐시) |
| `icon-192.png` | Android 홈 화면 / 작업 표시줄 아이콘 |
| `icon-512.png` | 스플래시 화면 아이콘 |
| `icon-maskable-512.png` | Android adaptive 아이콘 |
| `apple-touch-icon.png` | iOS 홈 화면 아이콘 (180×180) |
| `favicon.svg` | 브라우저 탭 아이콘 (벡터) |
| `README.md` | 이 문서 |

외부 라이브러리·웹폰트·CDN을 **하나도 사용하지 않습니다.** 그래서 네트워크가 완전히
끊긴 상태에서도 100% 동작합니다.

---

## 2. 사용법

### 기본 조작

| 조작 | 결과 |
|---|---|
| 문구 본문 탭/클릭 | 클립보드에 복사 |
| 오른쪽 아래 **＋** | 문구 추가 (목록 최상단에 들어갑니다) |
| 문구 왼쪽 `⋮` | **카드 메뉴** — 위/아래 이동, 맨 위로, 맨 아래로, 편집, 삭제 |
| 헤더 🔍 | 검색 |
| 헤더 ☀/🌙 | 테마 전환 (시스템 → 라이트 → 다크 → 시스템) |
| 헤더 ⋯ | 그룹 관리, 가나다순 정렬, 목록 열 수, 스크롤 위치 기억, JSON 내보내기…·가져오기, 전체 삭제, 치환 변수, 앱 정보 |

### 카드 메뉴 (⋮)

문구 왼쪽의 **⋮** 를 누르면 그 문구에 대한 동작이 한 번에 나옵니다.

```
↑  위로 이동
↓  아래로 이동
⤒  맨 위로
⤓  맨 아래로
───────────
✎  편집
🗑  삭제
```

- **위로/아래로 는 눌러도 메뉴가 닫히지 않습니다.** 메뉴가 문구를 따라 움직이므로
  여러 칸을 연속해서 옮길 수 있습니다.
- **맨 위로 / 맨 아래로** 는 메뉴를 닫고 옮겨진 문구가 보이는 자리로 이동합니다.
- 목록의 끝에 있으면 그 방향 항목은 흐리게 표시되고 눌리지 않습니다.
- **삭제는 5초간 실행 취소** 할 수 있습니다(원래 자리로 되돌아갑니다).
- PC에서는 문구를 고른 뒤 **`Alt`+`↑`** / **`Alt`+`↓`** 로 메뉴를 열지 않고 바로 옮길 수 있습니다.

> 이전 버전의 **끌어서 순서 바꾸기(`⠿`)** 는 없어졌습니다. 손가락으로 끌 때 화면 스크롤과
> 충돌해 잘 듣지 않았기 때문입니다. 대신 문구 왼쪽 영역에서도 **위아래 스크롤이 정상 동작**
> 하고, 카드 아래에 있던 버튼 줄이 사라져 **한 화면에 보이는 문구가 두 배 가까이** 늘었습니다.

### 그룹으로 나눠 담기

문구가 많아지면 **주소 / 계좌 / 인사말** 처럼 종류별로 나눠 담을 수 있습니다.
그룹을 하나라도 만들면 헤더 아래에 **그룹 칩 바**가 나타납니다.

```
⬤전체 12   주소 3   계좌 2   미분류 1  ＋
```

- 칩을 한 번 누르면 그 그룹만 보입니다. 그룹이 많아지면 칩 바를 **좌우로 밀어** 넘깁니다.
- 칩 바 끝의 **＋** 또는 **⋯ → 그룹 관리** 로 그룹을 만듭니다. 그룹 관리에서 이름 변경,
  칩 표시 순서 변경, 삭제를 할 수 있습니다.
- **한 문구는 한 그룹에만** 속합니다. 문구를 추가·편집할 때 창 아래의 **그룹** 에서 고릅니다.
  특정 그룹을 보고 있으면 그 그룹이 기본값이 됩니다.
- 그룹을 지정하지 않은 문구는 **미분류** 로 모입니다.
- **그룹을 지워도 그 안의 문구는 사라지지 않습니다** — 미분류로 옮겨지며, 실행 취소로
  그룹과 소속을 함께 되살릴 수 있습니다.
- 그룹이 하나도 없으면 칩 바가 나타나지 않습니다. 그룹을 쓰지 않으면 화면이 이전과 같습니다.

**순서 변경은 그룹 안에서만 일어납니다.** 어떤 그룹에서 카드 메뉴(⋮)로 옮겨도
다른 그룹의 순서는 그대로 유지됩니다. 가나다순 정렬도 보고 있는 그룹만 정렬합니다
(`전체` 에서 실행하면 전부 정렬).

**검색은 보고 있는 그룹 안에서** 찾습니다. 그 그룹에 결과가 없으면
"전체에서 N건 찾았습니다"와 함께 **전체에서 보기** 버튼이 나오고, 결과가 있어도 다른
그룹에 더 있으면 목록 아래에 알려 줍니다.

### 가나다순 정렬

수동으로 옮기는 대신 **⋯ → 가나다순 정렬** 을 누르면 문구 내용을 기준으로 목록
전체를 한 번에 다시 정렬합니다. **오름차순(ㄱ→ㅎ)** 과 **내림차순(ㅎ→ㄱ)** 중에서
고를 수 있습니다.

- 대소문자를 구분하지 않고, 앞뒤 공백과 줄바꿈은 무시합니다.
- 숫자는 글자가 아니라 크기로 비교합니다(`2번` 이 `10번` 보다 앞).
- 정렬 직후 토스트의 **실행 취소** 를 누르면 이전 순서로 되돌아갑니다.
- 이미 그 순서면 아무것도 바꾸지 않고 "이미 정렬된 상태입니다" 를 알립니다.

> 검색으로 목록이 걸러진 상태에서는 순서 변경이 잠깁니다(카드 메뉴의 이동 항목 4개가
> 흐려집니다. 편집·삭제는 그대로 됩니다). 보이지 않는 항목 사이로 옮기면 결과를
> 예측할 수 없기 때문입니다. 검색어를 지우면 다시 풀립니다.

### 목록 열 수

문구를 **몇 줄로 늘어놓을지** 고를 수 있습니다. **⋯ → 목록 열 수** 에서
**자동 / 1열 / 2열 / 3열 / 4열** 중 하나를 고릅니다(오른쪽에 현재 설정이 표시됩니다).

- **자동**(처음 설정) — 화면이 넓으면 2열, 좁으면 1열로 알아서 바뀝니다.
- **고른 값은 상한입니다.** 화면이 좁으면 자동으로 줄어들므로, 폰에서 4열을 골라도
  문구가 찌그러지지 않습니다. 예를 들어 `4열` 로 두면 폰에서는 1열, 태블릿에서는 2~3열,
  넓은 PC 화면에서는 4열로 보입니다.
- 설정은 **그 기기에만** 저장됩니다. JSON 백업에는 들어가지 않으므로 다른 기기로 옮겨도
  그 기기에서 보던 화면 배치가 바뀌지 않습니다.

### 문구마다 다른 색

비슷한 내용이 이어질 때 읽지 않고 구분할 수 있도록, 문구마다 **카드 색**을 지정할 수 있습니다.
문구를 추가·편집하는 창의 **색상** 줄에서 기본 8색 중에 고르거나 **직접 고르기** 로
원하는 색을 지정합니다(**없음** 을 고르면 색이 빠집니다).

고른 색을 그대로 칠하지 않고 **바탕색에 옅게 섞어** 쓰므로, 어떤 색을 골라도 글자가
묻히지 않습니다. 원색은 카드 왼쪽의 얇은 색 띠에만 쓰입니다. 색을 한 번도 쓰지 않으면
화면은 이전과 똑같습니다.

### 날짜·시각 자동 채우기 (치환 변수)

문구 안에 `{yyyy-MM-dd}` 처럼 적어 두면 **복사할 때** 그 자리가 현재 날짜·시각으로 바뀝니다.
저장된 문구 자체는 그대로 남습니다.

```
/rename {yyyyMMdd_HHmm}   →   /rename 20260914_0955
```

- 카드 아래에 **지금 복사하면 어떻게 되는지** 미리보기가 한 줄로 표시됩니다.
- 쓸 수 있는 기호는 **⋯ → 치환 변수 (날짜·시각)** 에서 현재 시각이 채워진 표로 확인합니다
  (문구를 편집하는 중에도 열 수 있습니다).
- 아는 기호가 없는 중괄호(`{메모}`)는 건드리지 않습니다.

### 스크롤 위치 기억

문구가 많아 스크롤해서 쓰는 경우, 앱을 닫았다 다시 열면 **마지막에 보고 있던 문구부터**
보이게 합니다. **⋯ → 스크롤 위치 기억** 에서 켜고 끌 수 있으며, 오른쪽에 현재 상태
(**켜짐** / **꺼짐**)가 표시됩니다. 처음 설치 상태는 **켜짐** 입니다.

- 화면 맨 위에 걸려 있던 **문구 자체를 기억**합니다. 그래서 그 사이 문구를 추가·삭제·
  정렬했더라도 같은 문구가 다시 나타납니다.
- **위치는 그룹별로 따로 기억합니다.** 주소 그룹을 보다가 계좌 그룹으로 옮긴 뒤 다시
  주소로 돌아오면, 주소에서 보던 자리로 돌아옵니다.
- **마지막에 보고 있던 그룹도 기억**합니다. 앱을 다시 열면 그 그룹이 선택된 채로,
  그 그룹에서 보던 위치부터 나타납니다.
- 그 문구를 지웠다면 비슷한 위치로 이동합니다. 목록이 짧아졌으면 갈 수 있는 끝까지만
  내려갑니다.
- **맨 위에서 시작하는 경우** — 위치를 기억하지 않았거나(끄기), 문구 추가·정렬·
  가져오기·전체 삭제처럼 목록을 위에서부터 다시 보게 되는 동작을 한 뒤, 또는
  마지막으로 본 시점에서 **12시간이 지난** 뒤입니다.
- 검색 중의 위치는 기억하지 않습니다(검색을 지운 뒤의 목록과 맞지 않기 때문입니다).

### 키보드 단축키 (PC)

| 키 | 동작 |
|---|---|
| `N` | 새 문구 추가 |
| `/` | 검색 |
| `Alt`+`↑` / `Alt`+`↓` | 고른 문구를 한 칸 위/아래로 이동 |
| `Ctrl+Enter` | 입력창에서 저장 |
| `Esc` | 창/메뉴/검색 닫기 |

---

## 3. 실행 방법

### 3.1 PC에서 그냥 열기

`index.html` 을 더블클릭하면 됩니다. 별도 서버나 설치가 필요 없습니다.

단, `file://` 로 열면 브라우저 정책상 **설치(PWA)와 오프라인 캐시 기능은 동작하지
않습니다.** 문구 저장·복사·순서 변경 등 나머지 기능은 모두 정상 동작합니다.

### 3.2 GitHub Pages 로 배포하기 (권장)

프로젝트 폴더(`..\`)에서 다음을 실행하면 GitHub Pages 저장소에 배포됩니다.

```
python 11_DeployToPages.py
```

- 배포 위치: `D:\Temp\gh\cwheo\Pages-WebApp\docs\QuickCopy`
- 앱 실행 주소: https://cwheo.github.io/Pages-WebApp/QuickCopy/
- 저장소 웹 주소: https://github.com/cwheo/Pages-WebApp/tree/main/docs/QuickCopy

보조 스크립트:

| 파일 | 용도 |
|---|---|
| `09_ChangeVersionName.py` | 버전 일괄 변경 (`index.html`·`sw.js`·매니페스트) |
| `001_Local_Repository_Pages_OpenFolder.bat` | 배포된 로컬 폴더 열기 |
| `001_Remote_Repository_GitHub_Pages.bat` | 저장소 웹 페이지 열기 |

### 3.3 스마트폰에 설치하기

1. 스마트폰 브라우저로 https://cwheo.github.io/Pages-WebApp/QuickCopy/ 에 접속합니다.
2. 앱을 홈 화면에 추가합니다.
   - **Android / Chrome**: 헤더의 **설치** 버튼 또는 메뉴의 "앱 설치"
   - **iPhone / Safari**: 하단 **공유** → **홈 화면에 추가**
3. 홈 화면에 QuickCopy 전용 아이콘이 생기고, 이후에는 **데이터 네트워크 없이도**
   실행됩니다(비행기 모드에서도 동작).

GitHub Pages 는 기본으로 HTTPS 를 제공하므로 PWA 설치 요건이 자동으로 충족됩니다.

> 다른 곳에 올려 쓰고 싶다면 이 **폴더 자체**를 정적 호스팅(예: https://app.netlify.com/drop)에
> 통째로 올리면 그대로 동작합니다. 별도 설정 파일은 필요하지 않습니다.

---

## 4. 데이터 백업 (중요)

문구는 브라우저의 `localStorage` 에 저장됩니다. 따라서 다음 상황에서 **사라질 수
있습니다.**

- 브라우저의 "인터넷 사용 기록/사이트 데이터 삭제" 실행
- 앱(홈 화면 아이콘) 삭제 후 재설치
- 기기 초기화 또는 교체
- 사생활 보호(시크릿) 모드에서 사용

### 백업하는 방법

1. 헤더 **⋯** → **JSON 내보내기…**
2. **저장할 파일 이름**을 입력합니다. 지난번에 쓴 이름이 미리 채워집니다.
   (`.json` 을 빼먹으면 자동으로 붙습니다.)
3. 이어서 **저장할 위치**를 고릅니다. 기기에 따라 화면이 다릅니다.

| 기기 | 다음 화면 | 저장 위치 |
|---|---|---|
| **PC (Chrome/Edge)** | 저장 대화상자 | 폴더를 직접 선택. 이름도 다시 고칠 수 있습니다 |
| **안드로이드 / iPhone** | 공유 시트 | **드라이브에 저장** 을 고르면 계정·폴더를 선택합니다 |
| 그 외 브라우저 | — | 기본 다운로드 폴더에 지정한 이름으로 저장 |

백업 파일에는 **그룹 목록과 각 문구의 소속도 함께** 들어갑니다.

> **취소해도 아무 일도 일어나지 않습니다.** 저장 대화상자나 공유 시트를 닫으면
> 조용히 종료되며 데이터는 그대로입니다.

### 복원 / 다른 기기로 옮기는 방법

1. 새 기기에서 앱을 열고 헤더 **⋯** → **JSON 가져오기**
2. 백업 파일을 선택합니다.
   - **PC** — 파일 대화상자에서 드라이브 폴더(`G:\내 드라이브\…`)로 이동
   - **안드로이드** — 파일 선택기 좌측 **☰** → **Drive** → 폴더 탐색
3. **덮어쓰기** 또는 **기존 뒤에 추가** 중에서 고릅니다.
   뒤에 추가할 때 **같은 이름의 그룹은 기존 그룹으로 합쳐집니다.**
4. 잘못 가져왔으면 화면 아래 토스트의 **실행 취소** 를 누르면 되돌아갑니다.

> 그룹이 없던 이전 버전의 백업 파일도 그대로 가져올 수 있습니다.
> 그 문구들은 **미분류** 로 들어갑니다.

### 구글 드라이브로 여러 기기에서 같은 설정 쓰기

모든 기기가 같은 구글 계정에 연결되어 있다면, 드라이브를 보관소로 쓰는 것이 가장 간단합니다.

1. 드라이브에 `QuickCopy` 폴더를 하나 만듭니다.
2. **PC** — [Google Drive for desktop](https://www.google.com/drive/download/) 을 설치하면
   탐색기에 `G:` 드라이브가 생깁니다. 내보내기 저장 대화상자에서 이 폴더를 고릅니다.
   (그 폴더를 탐색기에서 **즐겨찾기에 고정**해 두면 매번 클릭 한 번입니다.)
3. **폰** — 내보내기 → 공유 시트 → **드라이브에 저장** → 같은 폴더 선택.
4. 용도별로 파일을 여러 개 두고(`업무용.json`, `개인용.json` …) 기기마다 원하는 것을
   가져다 쓰면 됩니다.

> **이름을 매번 같게** 쓰면 파일이 쌓이지 않고 최신 상태 하나만 유지됩니다.
> 실수로 덮어썼더라도 드라이브가 이전 버전을 보관하므로 되돌릴 수 있습니다.

> 기기 간 **자동** 동기화 기능은 없습니다(서버가 없기 때문입니다).
> JSON 파일이 곧 이동 수단입니다.

---

## 5. 자주 있는 질문

**Q. 복사가 안 됩니다.**
브라우저가 클립보드 접근을 막은 경우입니다. 앱은 자동으로 예비 수단을 시도하고,
그마저 막히면 "수동으로 복사해 주세요" 창을 띄워 내용을 선택된 상태로 보여줍니다.
`Ctrl+C` 로 복사하면 됩니다. HTTPS(Netlify 주소)로 접속하면 대부분 해결됩니다.

**Q. 문구를 수정했는데 앱에서 옛 화면이 보입니다.**
Service Worker 캐시 때문입니다. 버전을 올린 뒤 다시 배포하세요.
프로젝트 폴더의 `09_ChangeVersionName.py` 의 `NEW_VERSION` 을 고쳐 실행하면
`index.html` 의 `APP_VERSION` 과 `sw.js` 의 `CACHE_NAME = 'quickcopy-v<버전>'` 이
함께 바뀝니다. 새 버전이 감지되면 앱이 "새 버전이 준비되었습니다 [새로고침]"
안내를 띄웁니다.

**Q. 저장 공간이 가득 찼다고 나옵니다.**
`localStorage` 한도(약 5MB)에 도달한 경우입니다. JSON으로 내보내 백업한 뒤
필요 없는 문구를 지우세요.

**Q. 아이콘을 바꾸고 싶습니다.**
`favicon.svg` 를 수정한 뒤, 프로젝트의 `tools\make_icons.py` 를 같은 기하로
맞춰 고치고 `python make_icons.py` 를 실행하면 PNG 4종이 다시 만들어집니다.
(Python + Pillow 필요)

---

## 6. 기술 요약

| 항목 | 내용 |
|---|---|
| 형태 | 서버 없는 정적 웹앱 |
| 프레임워크 | 없음 (Vanilla JS, IIFE 스코프) |
| 외부 의존성 | 0개 |
| 저장소 | `localStorage` (`quickcopy.data.v1` = 그룹·문구, `quickcopy.pref.v1` = 테마·목록 열 수·그룹별 스크롤 위치) |
| 데이터 스키마 | `schema: 3` (그룹 + 문구별 색상). 이전 `schema: 1`·`2` 데이터는 열 때 자동으로 올라갑니다 |
| 오프라인 | Service Worker 앱 셸 프리캐시 (Cache-First) |
| 대상 브라우저 | Chrome / Edge / Safari(iOS) / Samsung Internet 최신 버전 |
| 문자 인코딩 | UTF-8 (BOM 없음) |

---

버전: 1.3.0

작성자: 허창원  
AI 지원: Claude Code Assistant
